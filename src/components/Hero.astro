---
import { ArrowRight, Calendar } from 'lucide-astro';

const calUrl = import.meta.env.PUBLIC_CAL_URL || '#';
---

<section class="section bg-gradient-to-b from-primary-50 to-white">
  <div class="container-custom">
    <div class="max-w-4xl mx-auto text-center space-y-8">
      <!-- Badge -->
      <div class="inline-flex items-center px-4 py-2 bg-primary-100 text-primary-700 rounded-full text-sm font-semibold">
        🚀 Modern digitális megoldások temetkezési vállalkozásoknak
      </div>

      <!-- Main Heading -->
      <h1 class="text-4xl md:text-6xl lg:text-7xl font-bold text-secondary-900 leading-tight">
        Tegye <span class="text-primary-600">digitálissá</span> temetkezési vállalkozását
      </h1>

      <!-- Subheading -->
      <p class="text-xl md:text-2xl text-secondary-600 max-w-3xl mx-auto leading-relaxed">
        Komplett ügyviteli rendszer e-Anyakönyvvel, online foglalással és automatizált folyamatokkal.
        Spóroljon időt, növelje az ügyfél-elégedettséget és modernizálja vállalkozását.
      </p>

      <!-- CTA Buttons -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center pt-4">
        <a href="#kapcsolat" class="btn-primary w-full sm:w-auto group">
          Kérek visszahívást
          <ArrowRight class="w-5 h-5 ml-2 group-hover:translate-x-1 transition-transform" />
        </a>
        <a
          href={calUrl}
          target="_blank"
          rel="noopener noreferrer"
          class="btn-outline w-full sm:w-auto group"
        >
          <Calendar class="w-5 h-5 mr-2" />
          Demó időpont foglalás
        </a>
      </div>

      <!-- Social Proof / Trust Indicators -->
      <div class="pt-8 md:pt-12">
        <div class="grid grid-cols-3 gap-8 max-w-2xl mx-auto">
          <div class="text-center">
            <div class="text-3xl md:text-4xl font-bold text-primary-600">50+</div>
            <div class="text-sm md:text-base text-secondary-600 mt-1">Elégedett ügyfél</div>
          </div>
          <div class="text-center">
            <div class="text-3xl md:text-4xl font-bold text-primary-600">98%</div>
            <div class="text-sm md:text-base text-secondary-600 mt-1">Ügyfél-elégedettség</div>
          </div>
          <div class="text-center">
            <div class="text-3xl md:text-4xl font-bold text-primary-600">24/7</div>
            <div class="text-sm md:text-base text-secondary-600 mt-1">Támogatás</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
